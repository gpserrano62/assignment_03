
This assignment had us create a streamlit to showcase the parse_packaging, calc_total_units, and get_unit functions to seperate package descriptions and calculate the final number of the total product. I learned how to open the different functions between other files. This was done by having the Also going through this I was refreshed on using the strip and splitlines methods to parse through the inputted information. The strip method removing all the white space from each line, then the splitlines method which splits up each of the words in the line into their own one. I had the most problems during this lab, once again with the indentation. I was thrown many errors because some of my text wasnt in line with the if uploaded_file function. Additionally I had a lot of confusion in loading the streamlit from the web browser, not straight from VS Code (because I use option A). This made me assume my code wasnt working, however I just couldnt get the streamlit loading from running the process_files.py file. Next time while completing my homework I will double check my indentation again and rerun my code more frequently to find smaller bugs before they become a bigger problem. 