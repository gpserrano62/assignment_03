Write your reflection here.

See reflection.md for what makes a good reflection — be specific, use the
terminology from this assignment (rerun, widget, return value, guard, bytes vs.
text, decode, session state, initialise once, button-triggered, metric), and make
it actionable.

This assignment had us create a streamlit to showcase the parse_packaging, calc_total_units, and get_unit functions to seperate package descriptions and calculate the final number of the total product. I learned how to open the different functions between other files. Also going through this I was refreshed on using the strip and splitlines methods to parse through the inputted information. I had the most problems during this lab, once again with the indentation. I was thrown many errors because one of my functions wasnt inline. Additionally I had a lot of confusion in loading the streamlit from the web browser, not straight from VS Code (because I use option A). This made me assume my code wasnt working, however I just couldnt get the streamlit loading. 